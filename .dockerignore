dist-types
node_modules
packages/*/dist
packages/*/node_modules
plugins/*/dist
plugins/*/node_modules
*.local.yaml
# Config files are never baked into the image.
# Mount them at runtime via Docker volume or Kubernetes ConfigMap/Secret.
app-config*.yaml